# Required: signing secret and submission endpoint (set in env or CI secrets; do not commit values)
SIGNING_SECRET=get-this-from-b12
SUBMISSION_URL=https://b12.io/apply/submission

# Required for submission (or set as GitHub Actions secrets)
NAME="Full Name"
EMAIL=email@example.com
RESUME_LINK=https://example.com/resume.pdf

# Optional: override repo/run links (otherwise derived from GITHUB_* in CI)
REPOSITORY_LINK=https://github.com/username/repo
ACTION_RUN_LINK=https://github.com/username/repo/actions/runs/run_id

# Set automatically in GitHub Actions; only needed for local runs or other CI
# GITHUB_SERVER_URL=https://github.com
# GITHUB_REPOSITORY=username/repo
# GITHUB_RUN_ID=123456789
